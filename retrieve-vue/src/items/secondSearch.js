class SecondSearch {
    constructor(page, source, text, imgUrl, paragraph, score, targetTimes) {
        this.page = page
        this.source = source
        this.text = text
        this.imgUrl = imgUrl
        this.paragraph = paragraph
        this.score = score
        this.targetTimes = targetTimes
    }
}

export default SecondSearch